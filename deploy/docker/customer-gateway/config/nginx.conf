server {
  listen 80;
  server_name _;

  location / {
    default_type application/json;
    add_header Content-Type application/json;
    return 200 '{"status":"ok","service":"customer-origin"}';
  }
}
